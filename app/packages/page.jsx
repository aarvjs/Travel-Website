import PackagesClient from "./PackagesClient";

export const metadata = {
  title: "Travel Packages — Curated Trips",
  description:
    "Browse WanderLux luxury travel packages. From Maldives retreats to Swiss alpine adventures — handcrafted itineraries for every type of traveler.",
};

export default function PackagesPage() {
  return <PackagesClient />;
}
